# -*- coding: utf-8 -*-
from django.conf.urls import patterns, url
from . import views

urlpatterns = patterns('myproject.myapp.views',
    url(r'^list/$', 'list', name='list'),
    url(r'^(?P<tmhelix_id>[0-2]+)/$', views.detail, name='detail'),
    url(r'^list/Clear/$', view='Clear', name='Clear')
)
